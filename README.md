# weather-app
A real-time Weather Dashboard built with vanilla JavaScript that fetches live data from the OpenWeatherMap API. It features dynamic background changes and UI updates based on current temperature levels.
